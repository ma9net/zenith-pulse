/**
 * Claims data-access layer — repository ports and their adapters.
 *
 * a data-access library exposes stores, repository ports,
 * and gateways, never templates.
 *
 * an MSW-backed adapter over synthetic data, and a `ClaimWorkbenchStore` signal
 * store. The port is declared here and implemented by an adapter so the domain never
 * learns where claims come from.
 */
export {};
