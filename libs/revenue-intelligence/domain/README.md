# revenue-intelligence-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build revenue-intelligence-domain` to build the library.

## Running unit tests

Run `nx test revenue-intelligence-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
